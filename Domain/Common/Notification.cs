using System;
namespace Domain.Common
{
    public class Notification
    {
    //    protected Notification()
    //    { }

    //    public Notification(UserLikes liker, UserLikes liked, string message)
    //    {
    //        UserLiker = liker ?? throw new ArgumentNullException(nameof(liker));
    //        UserLiked = liked ?? throw new ArgumentNullException(nameof(liked));
    //        Message = message ?? throw new ArgumentNullException(nameof(message));

    //        HasBeenReadByRecipient = false;
    //        NotificationDate = DateTimeOffset.Now;
    //    }

    //    public virtual UserLikes UserLiker { get; protected set; }
    //    public virtual UserLikes UserLiked { get; protected set; }
    //    public virtual string Message { get; protected set; }
    //    public virtual bool HasBeenReadByRecipient { get; protected set; }
    //    public virtual DateTimeOffset NotificationDate { get; protected set; }

    //    public virtual void MarkAsRead()
    //    {
    //        if (!HasBeenReadByRecipient)
    //        {
    //            HasBeenReadByRecipient = true;
    //        }
    //    }

    //    public virtual void MarkAsUnread()
    //    {
    //        if (HasBeenReadByRecipient)
    //        {
    //            HasBeenReadByRecipient = false;
    //        }
    //    }
    //}
}
